# MapSyncer 功能测试文档

本文档用于记录 MapSyncer for XaeroWorldmap 模组的功能测试结果。

**测试环境要求**：
- Minecraft 1.21.X + NeoForge
- Xaero's World Map mod（最低版本 1.37.0）
- 服务端和客户端均安装 MapSyncer

---

## 一、服务端生成功能测试

### 1.1 服务端生成命令测试

| 测试项 | 测试命令 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 全维度生成 | `/mapsyncer generate` | 生成所有已加载维度的地图缓存 | 通过 | |
| 单维度生成-主世界 | `/mapsyncer generate overworld` | 仅生成主世界缓存 | 通过 | |
| 单维度生成-地狱 | `/mapsyncer generate the_nether` | 仅生成地狱缓存 | 有待改进 | 生成的文件存在一定不同，客户端看着像被拦腰斩断一样，区块中间出现了竖线 |
| 单维度生成-末地 | `/mapsyncer generate the_end` | 仅生成末地缓存 | ✅ 已修复 | 虚空区域正确显示深紫色，未生成区块跳过渲染 |
| 单区域生成 | `/mapsyncer generate overworld 0 0` | 生成指定坐标的单个区域 | 通过 |  |
| 强制生成 | `/mapsyncer generate overworld force` | 强制重新生成，忽略缓存 | 通过 |  |
| 生成状态查询 | `/mapsyncer status` | 显示当前生成进度和状态 | 通过 | |
| Mod 维度识别 | 安装含自定义维度的 mod | 命令建议中显示完整维度 ID（namespace:path） | ✅ 通过 | 暮色森林显示 twilightforest:twilight_forest |
| Mod 维度生成 | `/mapsyncer generate twilightforest:twilight_forest` | 成功生成 mod 维度缓存 | ✅ 通过 | 使用 twilightforest$twilight_forest 格式保存 |
| 维度路径格式 | 检查原版传统格式和 Mod dimensions/ 格式 | 自动检测并正确使用 | ✅ 已测试 | 原版：region/, DIM-1/, DIM1/；Mod：dimensions/<ns>/<path>/ |
| 首次转换写入配置 | 检查配置文件 region_folder | 检测结果写入配置 | ✅ 已测试 | 自动检测并写入配置 |

**测试结果填写区**：
```
暮光森林维度测试通过：
- 命令格式：使用完整维度 ID（twilightforest:twilight_forest）
- 服务端生成：缓存路径 server_map_cache/twilightforest$twilight_forest/
- 客户端同步：保存路径 Multiplayer_server/twilightforest$twilight_forest/mw$worldId/
- 路径格式：原版使用传统格式（region/, DIM-1/, DIM1/），Mod 使用 dimensions/<namespace>/<path>/ 格式

v3.8 更新：
- 原版维度路径格式修正：始终使用传统格式，不尝试 dimensions/minecraft/
- 服务端命令使用 DimensionArgument 解决冒号解析问题
- 客户端命令建议显示 Mod 维度完整 ID
- 原版维度使用简化名称（overworld, the_nether, the_end）
```

---

### 1.2 洞穴模式生成测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 洞穴模式配置与目录 | 配置 CAVE 模式 + 检查 caves/<layer>/ 目录结构 | 配置生效，目录结构正确 | 通过 | cave_start=63, layer=3 |
| 洞穴层文件命名与内容 | 检查 zip 文件命名 + Xaero 地图查看洞穴层 | 格式正确，显示洞穴地形 | 通过 | 内容有一定异常 |
| 洞穴+地表共存 | 同时生成地表和洞穴 | 两个目录都存在文件 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写洞穴模式测试结果]
```

---

### 1.3 region_folder 配置测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 自定义 MCA 路径 | 设置 region_folder="DIM-1" + 检查日志 | 从正确路径读取 MCA，日志显示 | ⬜ 未测试 | |
| 错误路径处理 | 设置不存在的 region_folder | 报错或 fallback | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写 region_folder 测试结果]
```

---

### 1.4 增量更新测试

| 测试项 | 测试命令 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 增量模式启用（TICK/SCHEDULED） | `/mapsyncer incremental tick/scheduled` | 按配置自动运行 | 需要改进 | 使用指令后并未同步更改配置文件中的值 |
| 增量模式禁用与状态 | `/mapsyncer incremental off` + `/status` | 停止更新 + 显示状态 | 通过 | |
| 变化检测 | 生成后再次生成 + 修改区块后增量更新 | 跳过未变化区域，仅更新变化区域 | ⬜ 未测试 | |

**测试结果填写区**：
```
使用指令后并未同步更改配置文件中的值，另未进行本地化处理
```

---

## 二、客户端同步功能测试

### 2.1 同步命令测试

| 测试项 | 测试命令 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 单维度同步 | `/mapsyncer sync` 或 `/mapsyncer sync <dim>` | 同步指定维度 | ✅ 已修复 | 使用 greedyString 解析维度参数 |
| 同步所有维度 | `/mapsyncer sync all` | 同步所有可用维度 | 通过 | |
| 维度别名与建议 | `/mapsyncer sync nether` + 输入命令查看建议 | 支持别名 + 显示维度列表 | ✅ 已修复 | Mod 维度建议从 DIMENSION_TYPE 注册表获取 |
| Mod 维度同步 | `/mapsyncer sync twilightforest:twilight_forest` | 同步 Mod 维度 | ✅ 已修复 | greedyString 正确解析冒号 |

**测试结果填写区**：
```
客户端 sync 命令已修复：
- 使用 greedyString 解析包含冒号的维度 ID
- 从 DIMENSION_TYPE 注册表推断维度 ID（移除 _type 后缀）
- 命令建议显示：overworld, the_nether, the_end, all + Mod 维度完整 ID
```

---

### 2.2 洞穴层同步测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 洞穴层同步与目录 | 同步地狱 + 检查客户端 caves/<layer>/ 目录 | 文件传输 + 目录正确 | ⬜ 未测试 | |
| 洞穴层显示与兼容 | Xaero 地图切换洞穴层 + 新旧客户端兼容 | 显示洞穴地形 + 旧客户端正常 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写洞穴层同步测试结果]
```

---

### 2.3 增量同步与缓存测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 增量比对 | 同步后再次同步 + 服务端更新后同步 | 跳过无变化区域 + 仅同步变化区域 | ⬜ 未测试 | |
| 时间戳缓存 | 检查 sync_timestamps.cache | 缓存正确保存 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写增量同步测试结果]
```

---

### 2.4 断点续传测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 中断恢复 | 断线重连 + 跨维度 + 大地图中断 | 从中断位置继续 + 保留进度 | ⬜ 未测试 | |
| 配置禁用续传 | `enableResumeSync=false` | 每次重新开始 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写断点续传测试结果]
```

---

### 2.5 速率限制与传输配置测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 速率限制 | `syncSpeedLimitKBps=500/0` | 按限制传输 / 最大速率 | ⬜ 未测试 | |
| 包大小配置 | `maxSyncPacketSize=5MB` | 分批大小正确 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写速率限制测试结果]
```

---

### 2.6 维度过滤测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 单维度精确同步 | `/mapsyncer sync nether` | 仅同步地狱，不 fallback | ⬜ 未测试 | |
| 维度不存在提示 | 同步未生成的维度 | 提示需要先生成 | ⬜ 未测试 | |
| 首次同步无数据 | 首次同步无本地文件 | 正常请求全部数据 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写维度过滤测试结果]
```

---

## 三、配置文件测试

### 3.1 服务端配置测试

| 测试项 | 配置内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 配置文件生成与加载 | 首次运行 + 修改后重新加载 | 生成配置文件 + 配置生效 | ⬜ 未测试 | 需重启服务器 |
| 增量更新模式配置 | `incrementalUpdateMode = "TICK"` | TICK 模式生效 | ⬜ 未测试 | |
| 维度扫描配置 | 添加 dimension_configs + 设置 default_scan_mode | 配置生效 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写服务端配置测试结果]
```

---

### 3.2 客户端配置测试

| 测试项 | 配置内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 调试与性能配置 | `enableDebugLogging=true` + `maxConcurrentRegions=8` | 显示调试信息 + 并发数正确 | ⬜ 未测试 | |
| 断点续传开关 | `enableResumeSync = false` | 禁用续传 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写客户端配置测试结果]
```

---

## 四、文件系统测试

### 4.1 服务端缓存目录测试

| 测试项 | 检查内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 目录创建与原版维度命名 | 检查 `server_map_cache/` + null/DIM-1/DIM1 | 目录自动创建 + Xaero 格式命名 | ⬜ 未测试 | |
| 缓存文件 | 检查 mca_timestamps.cache + generation_cache.properties | 文件存在 + Properties 格式 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写服务端缓存目录测试结果]
```

---

### 4.2 客户端地图目录测试

| 测试项 | 检查内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 服务器与World ID目录 | Multiplayer_<server>/ + mw$<worldId>/ | 正确识别服务器 + 使用正确 worldId | ⬜ 未测试 | |
| 洞穴层与缓存目录 | caves/<layer>/ + sync_timestamps.cache | 洞穴层目录正确 + 缓存文件存在 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写客户端地图目录测试结果]
```

---

## 五、网络协议测试

### 5.1 数据传输测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 传输显示与完整性 | 进度显示 + 分批传输 + 数据验证 + caveLayer | 百分比显示 + 不卡顿 + 文件完整 + layer正确 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写网络协议测试结果]
```

---

### 5.2 错误处理测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 异常场景处理 | 无缓存 + 断开 + 服务重启 + 网络中断 | 正确提示或恢复 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写错误处理测试结果]
```

---

## 六、兼容性测试

### 6.1 Mod 与版本兼容性测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| Mod 兼容 | Xaero + C2ME + Chunky + 自定义维度 mod | 正常集成/保存/同步/识别 | ⬜ 未测试 | |
| 版本兼容 | 新服务端+旧客户端 + 旧服务端+新客户端 + 不同MC版本 | 向后兼容 + 正常同步 + 正常工作 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写 Mod 兼容性测试结果]
```

---

## 七、性能测试

### 7.1 生成与同步性能测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 生成性能 | 大区域生成 + 并发数 + 内存占用 | 合理时间完成 + 按配置 + 不超限 | ⬜ 未测试 | 记录耗时 |
| 同步性能 | 哈希计算 + 传输速度 + 地图刷新 | 合理时间 + 按速率 + 不卡顿 | ⬜ 未测试 | 并行度限制为2 |

**测试结果填写区**：
```
记录具体耗时数据：
- 100 区域生成：___秒
- 100 区域哈希计算：___秒
- 100 区域同步传输：___秒（速率限制___KB/s）
```

---

## 八、边界情况测试

### 8.1 极端情况与错误恢复测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 极端情况 | 空维度 + 负高度洞穴 + 超大坐标 + 空配置文件 | 正确处理 | ⬜ 未测试 | caves/-4/ 目录 |
| 错误恢复 | 单区块失败 + 磁盘空间不足 + 无写入权限 | 不崩溃 + 提示错误 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写边界情况测试结果]
```

---

## 九、渲染效果测试

### 9.1 地图显示与已知问题测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 原版维度地形 | 主世界 + 地狱 + 末地 | 地形正确显示 | ⚠️ 有问题 | 地狱竖线，末地已修复 |
| 洞穴层与颜色 | 洞穴层切换 + 方块颜色 + 生物群系 | 各层正确 + 颜色基本正确 + 边界平滑 | ⬜ 未测试 | |
| 已知问题验证 | 含水方块 + 树木 + 水体 + Mod方块 | 记录差异程度 | ⬜ 未测试 | |

**测试结果填写区**：
```
已发现的渲染问题：
- 地狱：区块中间出现竖线，像被拦腰斩断
- 末地：✅ 已修复 - 虚空区域正确显示深紫色，未生成区块跳过渲染
- 洞穴：生成的文件存在一定异常
```

---

## 十、综合测试总结

### 测试环境信息

| 项目 | 信息 |
|------|------|
| Minecraft 版本 | |
| NeoForge 版本 | |
| Xaero's World Map 版本 | |
| MapSyncer 版本 | |
| 服务端类型 | |
| 其他安装的 Mod | |

---

### 测试结果统计

| 类别 | 通过数 | 失败数 | 未测试数 | 备注 |
|------|--------|--------|----------|------|
| 服务端生成功能 | 9 | 1 | 7 | 末地已修复，地狱仍有问题，Mod维度测试通过 |
| 客户端同步功能 | 6 | 0 | 12 | sync 命令参数解析已修复 |
| 配置文件 | 0 | 0 | 8 | |
| 文件系统 | 0 | 0 | 4 | |
| 网络协议 | 0 | 0 | 2 | |
| 兼容性 | 0 | 0 | 2 | |
| 性能 | 0 | 0 | 2 | |
| 边界情况 | 0 | 0 | 2 | |
| 渲染效果 | 0 | 2 | 2 | |
| **总计** | **15** | **3** | **37** | |

---

### 发现的问题汇总

```
问题 1：地狱渲染异常
- 描述：地狱区块中间出现竖线分割
- 严重程度：中
- 复现步骤：`/mapsyncer generate the_nether` 后查看客户端地图
- 预期结果：地形完整显示
- 实际结果：区块中间出现竖线

问题 2：末地渲染异常
- 描述：末地外岛边缘区块被填充绿色，虚空应使用深紫色
- 严重程度：中 → ✅ 已修复
- 复现步骤：`/mapsyncer generate the_end` 后查看客户端地图
- 预期结果：地形完整显示
- 实际结果：虚空区域正确显示深紫色
- 修复方案：
  1. 空白像素写入完整 AIR 方块状态 + null biome，空白 Tile 也写入完整数据
  2. 添加区块存在性检测，区分区块未生成与区块内虚空区域
  3. 区块不存在 → 写入空 Tile（tileMarker = -1），客户端跳过渲染
  4. 区块存在但像素为虚空 → 写入 AIR + void（渲染深紫色）

问题 3：客户端同步目录识别
- 描述：单维度同步提示"无法确定目录，是否已连接至服务器"
- 严重程度：高
- 复现步骤：`/mapsyncer sync` 或 `/mapsyncer sync overworld`
- 预期结果：正常同步指定维度
- 实际结果：无法识别目录

问题 4：增量更新指令配置同步
- 描述：使用指令后未同步更改配置文件中的值
- 严重程度：低
- 复现步骤：`/mapsyncer incremental tick` 或 `scheduled`
- 预期结果：配置文件同步更新
- 实际结果：仅运行时生效，配置未更新

问题 5：缓存键格式不一致（已修复）
- 描述：旧版本客户端缓存使用 Minecraft 维度名，与服务端 Xaero 格式不匹配
- 严重程度：高 → ✅ 已修复
- 复现步骤：查看客户端 `sync_timestamps.cache` 文件
- 预期结果：使用 Xaero 格式（如 `null/-1_-1`, `DIM-1/-1_-1`）
- 实际结果（旧版本）：使用 Minecraft 格式（如 `overworld/-1_-1`, `the_nether/-1_-1`）
- 修复方案：
  1. 确认服务端和客户端缓存键均使用 Xaero 格式
  2. `overworld` → `null`, `the_nether` → `DIM-1`, `the_end` → `DIM1`
  3. Mod 维度使用 `namespace$path` 格式
  4. 删除旧缓存文件让其重新生成
```

---

### 待改进建议

```
建议 1：修复地狱渲染问题，检查区块边界处理逻辑（末地已修复，包括区块存在性检测）

建议 2：修复客户端同步目录识别问题，改进 XaeroMapIntegrator 服务器目录检测（已修复但需测试）

建议 3：增量更新指令调用后同步保存配置文件（已修复但需测试）

建议 4：维度注册改为首次执行地图转换时自动检测并写入配置（已实现但需测试）

建议 5：末地虚空渲染已修复，空白像素写入完整 AIR 方块状态，未生成区块跳过渲染

建议 6：缓存键格式已统一为 Xaero 格式，旧版本生成的缓存文件需删除重新生成
```

---

### 测试完成确认

| 项目 | 状态 | 签名 | 日期 |
|------|------|------|------|
| 测试执行完成 | ⬜ | | |
| 问题记录完成 | ✅ | | |
| 文档更新完成 | ✅ | | |

---

**测试文档版本**: 2.7
**最后更新**: 2026-05-20