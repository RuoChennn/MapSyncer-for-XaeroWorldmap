# MapSyncer 功能测试文档

本文档用于记录 MapSyncer for XaeroWorldmap 模组的功能测试结果。

**测试环境要求**：
- Minecraft 1.21.X + NeoForge
- Xaero's World Map mod（最低版本 1.37.0）
- 服务端和客户端均安装 MapSyncer

---

## 一、服务端生成功能测试

### 1.1 基础生成命令测试

| 测试项 | 测试命令 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 全维度生成 | `/mapsyncer generate` | 生成所有已加载维度的地图缓存 | 通过 | |
| 单维度生成-主世界 | `/mapsyncer generate overworld` | 仅生成主世界缓存 | 通过 | |
| 单维度生成-地狱 | `/mapsyncer generate the_nether` | 仅生成地狱缓存 | 有待改进 | 生成的文件存在一定不同，客户端看着像被拦腰斩断一样，区块中间出现了竖线 |
| 单维度生成-末地 | `/mapsyncer generate the_end` | 仅生成末地缓存 | 存在问题 | 末地外岛的边缘区块不知道为什么被填充上了绿色，末地的空白处本应是虚空，使用一个比较深的紫色 |
| 单区域生成 | `/mapsyncer generate overworld 0 0` | 生成指定坐标的单个区域 | 通过 |  |
| 强制生成 | `/mapsyncer generate overworld force` | 强制重新生成，忽略缓存 | 通过 |  |
| 生成状态查询 | `/mapsyncer status` | 显示当前生成进度和状态 | 通过 | |

**测试结果填写区**：
```

在服务器启动，加载mod的时候就应将原版三维度，可能存在的mod维度都进行注册，并且加入配置文件

``` 

---

### 1.2 Mod 维度生成测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| Mod 维度识别 | 安装含自定义维度的 mod | 命令建议中显示 mod 维度 | 通过 | 在暮色森林测试，可以正常显示twilight_forest |
| Mod 维度生成 | `/mapsyncer generate <mod:dimension>` | 成功生成 mod 维度缓存 | ⬜ 待测试 | 暮色森林修改了存档文件路径，新增自动检测功能 |
| Mod 维度目录命名 | 检查缓存目录结构 | 使用 `dimensions/<namespace>/<path>` 格式 | ⬜ 待测试 | Minecraft 26.1+ 新格式 |
| 新格式自动检测 | 检查 dimensions/minecraft/overworld | 自动检测新格式路径 | ⬜ 待测试 | 优先检测新格式 |
| 传统格式回退 | 新格式不存在时检测 DIM-1 | 正确回退到传统格式 | ⬜ 待测试 |
| 首次转换写入配置 | 检查配置文件 region_folder | 检测结果写入配置 | ⬜ 待测试 |

**测试结果填写区**：
```
[在此填写 Mod 维度测试结果]
```

---

### 1.3 洞穴模式生成测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 洞穴模式配置 | 在 mapsyncer-server.toml 设置地狱 CAVE 模式 | 配置生效 | 通过 | cave_start=63 |
| 洞穴层目录结构 | 检查 `server_map_cache/DIM-1/caves/<layer>/` | 存在 caves 子目录 | 通过 | |
| 洞穴层文件命名 | 检查生成的 zip 文件 | 格式为 `regionX_regionZ.zip` | 通过 | |
| 洞穴层计算 | 验证 layer = cave_start >> 4 | layer 值正确 | 通过 | 63 >> 4 = 3 |
| 洞穴内容验证 | 打开 Xaero 地图查看洞穴层 | 显示洞穴地形 | 通过 | 但是生成的文件存在一定异常 |
| 洞穴+地表共存 | 同时生成地表和洞穴 | 两个目录都存在文件 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写洞穴模式测试结果]
```

---

### 1.4 region_folder 配置测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 自定义 MCA 路径 | 设置 region_folder="DIM-1" | 从正确路径读取 MCA | ⬜ 未测试 | |
| 日志验证 | 检查日志输出 | 显示 "Using configured region_folder" | ⬜ 未测试 | |
| 错误路径处理 | 设置不存在的 region_folder | 报错或 fallback | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写 region_folder 测试结果]
```

---

### 1.5 增量更新测试

| 测试项 | 测试命令 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| TICK 模式启用 | `/mapsyncer incremental tick` | 每 200 ticks 自动扫描 | 需要改进 | 使用指令后并未同步更改配置文件中的值。需要同步更新配置文件 |
| TICK 模式间隔配置 | 修改 `incrementalUpdateIntervalTicks` | 按配置间隔运行 | ⬜ 未测试 | |
| SCHEDULED 模式启用 | `/mapsyncer incremental scheduled` | 每日指定时间运行 | 需要改进 | 使用指令后并未同步更改配置文件中的值。需要同步更新配置文件 |
| 定时时间配置 | 设置 `scheduledUpdateHour=4` | 04:00 自动运行 | ⬜ 未测试 | |
| 禁用增量更新 | `/mapsyncer incremental off` | 停止自动更新 | 通过 | |
| 状态查询 | `/mapsyncer incremental status` | 显示当前模式状态 | ⬜ 未测试 | |
| 未变化跳过 | 生成后再次生成 | 跳过已处理的区域 | ⬜ 未测试 | |
| MCA 变化检测 | 修改区块后增量更新 | 仅更新变化区域 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写增量更新测试结果]
使用指令后并未同步更改配置文件中的值，另未进行本地化处理
```

---

## 二、客户端同步功能测试

### 2.1 基础同步命令测试

| 测试项 | 测试命令 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 同步当前维度 | `/mapsyncer sync` | 同步玩家所在维度 | 不通过 | 玩家位于overworld，但客户端提示无法确定目录，是否已连接至服务器|
| 同步指定维度 | `/mapsyncer sync overworld` | 仅同步主世界 | 不通过 | 玩家位于overworld，但客户端提示无法确定目录，是否已连接至服务器 |
| 同步所有维度 | `/mapsyncer sync all` | 同步所有可用维度 | 通过 | 维度：all，但此处应当提示用户具体同步了哪些维度 |
| 维度别名测试 | `/mapsyncer sync nether` | 支持多种别名 | 不通过 | 但客户端提示无法确定目录，是否已连接至服务器 |
| 维度目录建议 | 输入命令查看建议 | 显示已有维度列表 | 原版通过 | mod服未知 |

**测试结果填写区**：
```
[在此填写基础同步测试结果]
```

---

### 2.2 洞穴层同步测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 洞穴层同步 | 同步地狱维度 | caves/<layer> 目录文件传输 | ⬜ 未测试 | |
| 洞穴层目录验证 | 检查客户端 `mw$worldId/caves/<layer>/` | 目录结构正确 | ⬜ 未测试 | |
| 洞穴层显示 | 打开 Xaero 地图切换洞穴层 | 显示洞穴地形 | ⬜ 未测试 | |
| caveLayer 协议兼容 | 服务端新/客户端旧 | 正常同步地表层 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写洞穴层同步测试结果]
```

---

### 2.3 增量同步测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 哈希匹配跳过 | 同步后再次同步 | 显示 "已同步，无需更新" | ⬜ 未测试 | |
| 时间戳比对 | 服务端更新后同步 | 仅同步变化区域 | ⬜ 未测试 | |
| 部分同步 | 仅部分区域变化 | 只传输变化部分 | ⬜ 未测试 | |
| 时间戳缓存 | 检查 sync_timestamps.cache | 缓存正确保存 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写增量同步测试结果]
```

---

### 2.4 断点续传测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 断线重连 | 同步中途断开连接 | 重连后从中断位置继续 | ⬜ 未测试 | |
| 跨维度中断 | 同步时跨维度 | 自动中止，保留进度 | ⬜ 未测试 | |
| 大地图续传 | 大量区域同步中断 | 续传功能正常 | ⬜ 未测试 | |
| 配置禁用 | 设置 `enableResumeSync=false` | 每次重新开始 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写断点续传测试结果]
```

---

### 2.5 速率限制测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 速率限制启用 | 设置 `syncSpeedLimitKBps=500` | 限制传输速率 | ⬜ 未测试 | |
| 无限制模式 | 设置 `syncSpeedLimitKBps=0` | 最大速率传输 | ⬜ 未测试 | |
| 大包配置 | 设置 `maxSyncPacketSize=5MB` | 分批大小正确 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写速率限制测试结果]
```

---

### 2.6 维度过滤测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 单维度精确同步 | `/mapsyncer sync nether` | 仅同步地狱，不 fallback | ⬜ 未测试 | |
| 维度不存在提示 | 同步未生成的维度 | 提示需要先生成 | ⬜ 未测试 | |
| 客户端无数据 | 首次同步无本地文件 | 正常请求全部数据 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写维度过滤测试结果]
```

---

## 三、配置文件测试

### 3.1 服务端配置测试

| 测试项 | 配置内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 配置文件生成 | 首次运行 | 生成 mapsyncer-server.toml | ⬜ 未测试 | |
| 增量更新模式 | `incrementalUpdateMode = "TICK"` | TICK 模式生效 | ⬜ 未测试 | |
| 维度扫描配置 | 添加 dimension_configs 条目 | 配置生效 | ⬜ 未测试 | |
| 默认扫描模式 | `default_scan_mode = "CAVE"` | 未配置维度使用 CAVE | ⬜ 未测试 | |
| 配置热加载 | 修改配置后重新加载 | 配置立即生效 | ⬜ 未测试 | 需重启服务器 |

**测试结果填写区**：
```
[在此填写服务端配置测试结果]
```

---

### 3.2 客户端配置测试

| 测试项 | 配置内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 调试日志 | `enableDebugLogging = true` | 显示详细调试信息 | ⬜ 未测试 | |
| 并发限制 | `maxConcurrentRegions = 8` | 并发数正确 | ⬜ 未测试 | |
| 断点续传开关 | `enableResumeSync = false` | 禁用续传 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写客户端配置测试结果]
```

---

## 四、文件系统测试

### 4.1 服务端缓存目录测试

| 测试项 | 检查内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 目录创建 | 检查 `server_map_cache/` | 目录自动创建 | ⬜ 未测试 | |
| 主世界目录 | 检查 `null/` | 使用 Xaero 格式命名 | ⬜ 未测试 | |
| 地狱目录 | 检查 `DIM-1/` | 使用 Xaero 格式命名 | ⬜ 未测试 | |
| 末地目录 | 检查 `DIM1/` | 使用 Xaero 格式命名 | ⬜ 未测试 | |
| 时间戳缓存 | 检查 `mca_timestamps.cache` | 文件存在 | ⬜ 未测试 | |
| 生成缓存 | 检查 `generation_cache.properties` | Properties 格式 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写服务端缓存目录测试结果]
```

---

### 4.2 客户端地图目录测试

| 测试项 | 检查内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 服务器目录 | 检查 `Multiplayer_<server>/` | 正确识别服务器 | ⬜ 未测试 | |
| World ID 目录 | 检查 `mw$<worldId>/` | 使用正确 worldId | ⬜ 未测试 | |
| 洞穴层目录 | 检查 `caves/<layer>/` | 洞穴层目录正确 | ⬜ 未测试 | |
| 时间戳缓存 | 检查 `sync_timestamps.cache` | 缓存文件存在 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写客户端地图目录测试结果]
```

---

## 五、网络协议测试

### 5.1 数据传输测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 进度显示 | 同步时观察聊天栏 | 显示百分比和进度 | ⬜ 未测试 | |
| 分批传输 | 大量数据同步 | 分批传输不卡顿 | ⬜ 未测试 | |
| 数据完整性 | 同步后验证文件 | 文件完整无损 | ⬜ 未测试 | |
| caveLayer 传输 | 洞穴层同步 | 正确传输 layer 信息 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写网络协议测试结果]
```

---

### 5.2 错误处理测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 服务端无缓存 | 同步未生成的维度 | 提示需先生成 | ⬜ 未测试 | |
| 客户端断开 | 同步中断开 | 服务端正确处理 | ⬜ 未测试 | |
| 服务端重启 | 同步中服务重启 | 客户端重新请求 | ⬜ 未测试 | |
| 网络异常 | 模拟网络中断 | 自动恢复或提示 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写错误处理测试结果]
```

---

## 六、兼容性测试

### 6.1 Mod 兼容性测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| Xaero World Map | 安装 Xaero mod | 正常集成 | ⬜ 未测试 | |
| C2ME 兼容 | 安装 C2ME mod | 强制保存正常工作 | ⬜ 未测试 | |
| Chunky 预生成 | 使用 Chunky 预生成 | 正常同步预生成区域 | ⬜ 未测试 | |
| 自定义维度 mod | 安装含新维度的 mod | 正常识别和生成 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写 Mod 兼容性测试结果]
```

---

### 6.2 版本兼容性测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 新服务端+旧客户端 | 服务端更新，客户端旧版本 | 向后兼容 | ⬜ 未测试 | |
| 旧服务端+新客户端 | 服务端旧版本，客户端更新 | 正常同步地表层 | ⬜ 未测试 | |
| Minecraft 版本 | 测试不同 MC 1.21.x 版本 | 正常工作 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写版本兼容性测试结果]
```

---

## 七、性能测试

### 7.1 生成性能测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 大区域生成 | 生成 100+ 区域 | 合理时间内完成 | ⬜ 未测试 | 记录耗时：___秒 |
| 并发转换 | 检查并发数配置 | 按配置并发运行 | ⬜ 未测试 | |
| 内存占用 | 观察生成时内存 | 不超出限制 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写生成性能测试结果]
记录具体耗时数据：
- 100 区域：___秒
- 500 区域：___秒
- 全地图：___秒
```

---

### 7.2 同步性能测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 哈希计算速度 | 大量文件哈希计算 | 合理时间完成 | ⬜ 未测试 | 并行度限制为2 |
| 同步传输速度 | 大数据量同步 | 按配置速率传输 | ⬜ 未测试 | |
| 地图刷新速度 | 同步后地图刷新 | 不卡顿游戏 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写同步性能测试结果]
记录具体数据：
- 100 区域哈希计算：___秒
- 100 区域同步传输：___秒（速率限制___KB/s）
```

---

## 八、边界情况测试

### 8.1 极端情况测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 空维度同步 | 同步无区块的维度 | 正确处理空数据 | ⬜ 未测试 | |
| 负高度洞穴 | 设置 cave_start=-64 | caves/-4/ 目录正确 | ⬜ 未测试 | |
|超大坐标 | 远距离区块生成 | 正常处理 | ⬜ 未测试 | |
| 空配置文件 | 删除配置文件 | 重新生成默认配置 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写极端情况测试结果]
```

---

### 8.2 错误恢复测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 单区块失败 | 模拟区块解析失败 | 不中断整体生成 | ⬜ 未测试 | |
| 文件写入失败 | 磁盘空间不足 | 错误处理不崩溃 | ⬜ 未测试 | |
| 权限问题 | 无写入权限 | 提示错误信息 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写错误恢复测试结果]
```

---

## 九、渲染效果测试

### 9.1 地图显示测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 主世界地形 | 查看生成的主世界地图 | 地形正确显示 | ⬜ 未测试 | |
| 地狱地形 | 查看生成的地狱地图 | 洞穴地形正确 | ⬜ 未测试 | |
| 末地地形 | 查看生成的末地地图 | 地形正确显示 | ⬜ 未测试 | |
| 洞穴层切换 | 切换不同洞穴层 | 各层正确显示 | ⬜ 未测试 | |
| 方块颜色 | 检查方块颜色准确性 | 颜色基本正确 | ⬜ 未测试 | |
| 生物群系 | 检查生物群系颜色 | 边界平滑过渡 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写渲染效果测试结果]
发现的渲染问题：
```

---

### 9.2 已知渲染问题验证

| 问题 | 测试内容 | 实际表现 | 测试结果 | 备注 |
|------|----------|----------|----------|------|
| 含水方块 | 检查含水方块渲染 | 记录差异程度 | ⬜ 未测试 | |
| 树木渲染 | 检查树木完整性 | 记录问题类型 | ⬜ 未测试 | |
| 水体颜色 | 对比原版水体 | 记录差异 | ⬜ 未测试 | |
| Mod 方块颜色 | 检查 mod 方块 | 记录不准确案例 | ⬜ 未测试 | |

**测试结果填写区**：
```
[在此填写已知问题验证结果]
```

---

## 十、综合测试总结

### 测试环境信息

| 项目 | 信息 |
|------|------|
| Minecraft 版本 | |
| NeoForge 版本 | |
| Xaero's World Map 版本 | |
| MapSyncer 版本 | |
| 服务端类型 | |
| 其他安装的 Mod | |

---

### 测试结果统计

| 类别 | 通过数 | 失败数 | 未测试数 | 备注 |
|------|--------|--------|----------|------|
| 服务端生成功能 | | | | |
| 客户端同步功能 | | | | |
| 配置文件 | | | | |
| 文件系统 | | | | |
| 网络协议 | | | | |
| 兼容性 | | | | |
| 性能 | | | | |
| 边界情况 | | | | |
| 渲染效果 | | | | |
| **总计** | | | | |

---

### 发现的问题汇总

```
[在此填写测试中发现的所有问题]

问题 1：
- 描述：
- 严重程度：
- 复现步骤：
- 预期结果：
- 实际结果：

问题 2：
- 描述：
- 严重程度：
- 复现步骤：
- ...

```

---

### 待改进建议

```
[在此填写测试后的改进建议]

建议 1：

建议 2：

...

```

---

### 测试完成确认

| 项目 | 状态 | 签名 | 日期 |
|------|------|------|------|
| 测试执行完成 | ⬜ | | |
| 问题记录完成 | ⬜ | | |
| 文档更新完成 | ⬜ | | |

---

**测试文档版本**: 1.0
**最后更新**: 2026-05-19