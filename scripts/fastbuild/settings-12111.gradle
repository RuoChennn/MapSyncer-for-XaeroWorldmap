// settings-12111.gradle — 专用于构建 Fabric 1.21.11 模块
// 隔离 Loom 1.15.4 (Yarn mappings) 与 Loom 1.9.2 (其他 Fabric 模块)
// 使用方式：gradlew.bat -c scripts/fastbuild/settings-12111.gradle build

pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
        maven { url = 'https://maven.fabricmc.net/' }
        maven { url = 'https://maven.aliyun.com/repository/public' }
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.9.0'
}

rootProject.name = 'mapsyncer'

// ── 抽象库层 ──
include 'libs:core'
include 'libs:platform-api'
include 'libs:common'
include 'libs:mc-1.21.11'

// ── Fabric 1.21.11 ──
include 'mc-1.21.11:fabric'
